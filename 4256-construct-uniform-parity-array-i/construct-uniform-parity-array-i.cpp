class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int n=nums1.size();
        // int parity=((nums1[0]-nums1[1])%2);
        // for(int i=1;i<n-1;i++){
        //     if((nums1[i]-nums1[i+1])!=parity)return false;
        // }
        // if(nums1[n-1]%2!=parity)return false;
        return true;
    }
};